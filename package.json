{
  "name": "investingmate",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "AWS_PROFILE=dev-investingmate sst start --stage local",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "test": "vitest run"
  },
  "devDependencies": {
    "aws-cdk-lib": "^2.50.0",
    "@serverless-stack/cli": "^1.18.4",
    "@serverless-stack/resources": "^1.18.4",
    "@tsconfig/node16": "^1.0.3",
    "vitest": "^0.25.7"
  },
  "workspaces": [
    "services"
  ],
  "dependencies": {
    "aws-sdk": "^2.1273.0",
    "uuid": "^8.3.2"
  }
}
